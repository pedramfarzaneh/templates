/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MyDirective.directive.tsDirective } from './my-directive.directive.ts.directive';

describe('Directive: MyDirective.directive.ts', () => {
  it('should create an instance', () => {
    const directive = new MyDirective.directive.tsDirective();
    expect(directive).toBeTruthy();
  });
});